double getPendapatanNasionalPengeluaran(
    double c, double i, double g, double x, double m) {
  return c + i + g + (x - m);
}

double getPendapatanNasionalPemasukan(double r, double w, double i, double p) {
  return r + w + i + p;
}
