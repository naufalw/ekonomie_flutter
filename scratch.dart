void main() async {
  // Dio dio = new Dio();
  // String fromCurrency = "USD";
  // String toCurrency = "IDR";
  // String symbolnya = "?symbols=$toCurrency&base=$fromCurrency";
  // String url = "https://api.exchangerate.host/";
  // // var dmin1 = Jiffy().subtract(days: 1).toString().split(" ")[0];
  // // var dmin2 = Jiffy().subtract(days: 2).toString().split(" ")[0];
  // // var dmin3 = Jiffy().subtract(days: 3).toString().split(" ")[0];
  // // var dmin4 = Jiffy().subtract(days: 4).toString().split(" ")[0];
  // // var dmin5 = Jiffy().subtract(days: 5).toString().split(" ")[0];
  // // var hargadmin1 = await dio.get("$url$dmin1$symbolnya");
  // // var hargadmin2 = await dio.get("$url$dmin1$symbolnya");
  // // var hargadmin3 = await dio.get("$url$dmin1$symbolnya");
  // // var hargadmin4 = await dio.get("$url$dmin1$symbolnya");
  // // var hargadmin5 = await dio.get("$url$dmin1$symbolnya");
  // // print(hargadmin1.data["rates"][toCurrency].round());
  // List dataChart = [];
  // for (int i = 1; i < 6; i++) {
  //   DateTime dminDT = Jiffy().subtract(days: i);
  //   var dmin = Jiffy().subtract(days: i).toString().split(" ")[0];
  //   var hargadmin = await dio.get("$url$dmin$symbolnya");
  //   var dataCurrency = hargadmin.data["rates"][toCurrency].round();
  //   dataChart.add(dataCurrency);
  // }
  // print(dataChart);
}
